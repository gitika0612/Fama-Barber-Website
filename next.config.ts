import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['www.toppik.com', 'peteandpedro.com', 'www.uklash.com'], // add all external image hosts you use here
  },
};

export default nextConfig;
